{
  "name": "duobooth",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A playful online photobooth — solo strips, or a shared strip with someone on another laptop, live.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "trystero": "^0.25.2"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
